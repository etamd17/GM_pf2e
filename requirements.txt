flask==3.0.0
gunicorn==21.2.0
gevent==26.5.0
markdown==3.5.1
PyYAML==6.0.1
markupsafe==2.1.3
reportlab==4.1.0
segno==1.6.6

# Cosmere character-sheet PDF import (AcroForm field extraction)
pypdf>=4.0

# Uploaded-image downscale + recompress (portraits, handouts, hero/crest) to
# bound the Railway volume. Used best-effort: if absent, originals save as-is.
Pillow>=10.0
